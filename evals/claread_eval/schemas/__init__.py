"""Evaluation schemas (post-cutover).

The legacy workflow / judge / rubric / prompt-variant / report / run /
dataset / grader schemas were physically deleted. The only kept schema is
:mod:`claread_eval.schemas.vocabulary`; import it directly.
"""
