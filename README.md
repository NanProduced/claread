# 新仓库文档草稿区

这里存放准备迁入新仓库的新版文档草稿。

与 `_migration_prep/` 其他文件不同，本目录下的内容是“候选迁移资产”。正式迁移时应按路径复制到新仓库，并在新仓库内继续校准。

## 规则

- 旧仓库原文档不直接修改。
- 这里的文档按多端 Claread 重新编写。
- 不写旧仓库临时决策过程，只写当前结论。
- 不包含真实密钥、个人本地路径、旧 AI 工具状态。

正式迁移时，只复制本目录下被最终确认的文档文件；不要复制 `_migration_prep/` 本身。
