# Select

角色：单值预设选项选择器。

推荐场景：Settings 偏好选择、模式选择、低频配置项。

变体：
- `size`: `sm | md | lg`
- `tone`: `default | panel | quiet`

a11y：
- 有可见标签时使用组件 `label`
- 无可见标签时必须传 `aria-label`

底座：`@base-ui/react/select`
