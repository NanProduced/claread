services:
  directus:
    image: directus/directus:11
    container_name: claread-directus
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "8055:8055"
    environment:
      SECRET: ${SECRET}
      PUBLIC_URL: ${PUBLIC_URL}
      ROOT_REDIRECT: ${ROOT_REDIRECT}
      ADMIN_EMAIL: ${ADMIN_EMAIL}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD}
      ADMIN_TOKEN: ${ADMIN_TOKEN}
      DB_CLIENT: pg
      DB_HOST: postgres
      DB_PORT: 5432
      DB_DATABASE: ${POSTGRES_DB}
      DB_USER: ${POSTGRES_USER}
      DB_PASSWORD: ${POSTGRES_PASSWORD}
      DB_EXCLUDE_TABLES: ${DB_EXCLUDE_TABLES}
      EXTENSIONS_PATH: ${EXTENSIONS_PATH}
      EXTENSIONS_AUTO_RELOAD: ${EXTENSIONS_AUTO_RELOAD}
      CHOKIDAR_USEPOLLING: ${CHOKIDAR_USEPOLLING}
      CLAREAD_API_BASE_URL: ${CLAREAD_API_BASE_URL}
      CLAREAD_API_ADMIN_KEY: ${CLAREAD_API_ADMIN_KEY}
      CLAREAD_EVAL_PROXY_TIMEOUT_MS: ${CLAREAD_EVAL_PROXY_TIMEOUT_MS}
      CLAREAD_EVAL_RUNS_ROOT: ${CLAREAD_EVAL_RUNS_ROOT}
      CLAREAD_NODE_LAB_ARTIFACTS_ROOT: ${CLAREAD_NODE_LAB_ARTIFACTS_ROOT}
      CLAREAD_WORKFLOW_RUNTIME_RUNS_ROOT: ${CLAREAD_WORKFLOW_RUNTIME_RUNS_ROOT:-/directus/runtime-evals/workflow-runs}
      CLAREAD_WORKFLOW_COMPARE_RUNTIME_ROOT: ${CLAREAD_WORKFLOW_COMPARE_RUNTIME_ROOT:-/directus/runtime-evals/workflow-compares}
      CLAREAD_NODE_LAB_JUDGE_DISPATCH_MODE: ${CLAREAD_NODE_LAB_JUDGE_DISPATCH_MODE:-directus_async}
      CLAREAD_NODE_LAB_JUDGE_WORKER_COMMAND: ${CLAREAD_NODE_LAB_JUDGE_WORKER_COMMAND:-python -m claread_eval.node_lab_judge.worker}
    entrypoint: >
      /bin/sh -c "
      mkdir -p /directus/runtime-evals/node-lab/sessions
      mkdir -p /directus/runtime-evals/workflow-runs
      mkdir -p /directus/runtime-evals/workflow-compares
      exec npx directus start
      "
    volumes:
      - ../../apps/directus/.runtime/uploads:/directus/uploads
      - ../../apps/directus/.runtime/evals:/directus/runtime-evals
      - ../../evals:/directus/evals:ro
      - ../../apps/directus/extensions/modules-bundle:/directus/extensions/directus-extension-claread-modules
      - ../../apps/directus/extensions/panels-bundle:/directus/extensions/directus-extension-claread-panels
      - ../../apps/directus/extensions/endpoints-bundle:/directus/extensions/directus-extension-claread-endpoints
      - ../../apps/directus/extensions/hooks-bundle:/directus/extensions/directus-extension-claread-hooks
